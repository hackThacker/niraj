//  Declare and initialize variables

        // Using var
        var varExample = "I am a variable declared with var.";
        document.getElementById("var").textContent = varExample;

        // Using let
        let letExample = "I am a variable declared with let.";
        document.getElementById("let").textContent = letExample;

        // Using const
        const constExample = "I am a variable declared with const.";
        document.querySelector(".const").textContent = constExample;
